请读取 `tests/fixtures/paper-chart-convergence.csv`，生成一张国赛论文风格的折线图：
- 横轴使用 `iteration`
- 纵轴同时展示 `baseline` 与 `improved`
- 标题为“算法收敛性能对比”
- 副标题为“基准方案与改进方案”
- 使用 academic-blue 主题
- 同时输出可编辑 HTML、SVG 和原生可编辑 PPTX
