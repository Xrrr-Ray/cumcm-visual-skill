# 已知问题

- 自动规划器是确定性规则系统，不能替代专家对叙事重点的最终判断。
- `data-chart` 是无外部依赖的轻量条形证据；复杂坐标轴与置信区间需人工扩展。
- 图片型页面不会联网搜索素材，只提供可替换区域；必须使用用户输入或已获授权图片。
- localStorage 按演示标题隔离；同源同标题副本可能共享状态，交付前应重置。
- Chrome 无头模式可能输出 USB 或系统 Web App stderr；以退出码、文件签名、尺寸和页数判断。
- 浏览器截图接口可能返回 JPEG，因此预览截图使用 `.jpg`；正式 PNG 由导出器生成并校验。

